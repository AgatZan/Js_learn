Используется для конструирование объектов, с использованием конструктор функции.

При применении new, [[this]] получит контекст нового объекта. 

`new.target` - возвращает undefind, если функция вызвана без new.