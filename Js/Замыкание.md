Способность получить значение из ограниченной области, на практике это осуществимо при помощи возвращения функции.


```js
function f(x){
let y = something;
return ()=>x*y;
or
return (...param)={
do something
y;
}
}
```
